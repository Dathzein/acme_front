// subjetivo por lo que no hay realmente ese ambiente productivo realmente
// mas que todo por si existiera :v
export const environment = {
  production: true,
  apiUrl: 'https://miapilaravel.produccion.com.co',
  version: '0.0.1',
}
